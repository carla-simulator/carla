#!/usr/bin/env groovy

pipeline
{
    agent none
    stages
    {
        stage('Configure')
        {
            steps {
              sh "ls"
            }
        }
        stage('Launch')
        {
            steps {
              sh "ls"
            }
        }
    }
}
