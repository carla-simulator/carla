#!/usr/bin/env groovy

pipeline
{
    agent { label "gpu" }
    stages
    {
        stage('Configure')
        {
            steps {
              sh "ls"
            }
        }
        stage('Launch')
        {
            steps {
              sh "ls"
            }
        }
    }
}
